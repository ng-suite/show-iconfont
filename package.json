{
  "name": "show-iconfont",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "read:file": "node ./src/index.js",
    "server": "lite-server -o -c ./lite-server.config.json",
    "start": "npm run read:file && npm run server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "font-carrier": "0.0.12",
    "lite-server": "^2.4.0"
  }
}
